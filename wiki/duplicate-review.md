# Duplicate Review

This page is for reviewing exact and likely duplicates found by the reconciliation process.

No duplicate should be deleted or merged until reviewed.

Current state:

- one exact duplicate hash group exists for the empty `manifest/agent_actions.jsonl` and `manifest/missing_log.jsonl` pair;
- eighteen exact duplicate pairs exist within the archived `artifacts/archived/Consent Crystal Structure Research/Semantic Collapse Theory/SCT set 20050505` snapshot, which likely represent a versioned snapshot relationship and can be collapsed into a coherent set after witness if the surviving record keeps the multiple-collapse-parent metadata.

Review those pairs before deciding whether any file should be merged, moved, labeled canonical, or collapsed into a single navigation set. File content should not be rewritten as part of that process.
